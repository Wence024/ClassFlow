# Skip postinstall scripts that may fail in CI/CD environments
# The supabase CLI postinstall script downloads binaries which can fail
# Using npx supabase works without having the package installed
ignore-scripts=false
