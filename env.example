# Environment Configuration Template
# Copy this file to .env.local for local development overrides
# DO NOT commit .env.local to Git (it's in .gitignore)

# Supabase Configuration
# These are placeholders - actual values are in committed .env.{environment} files
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_PROJECT_ID=your_supabase_project_id

# Environment Identifier
# Options: development, staging, production
VITE_APP_ENV=development

# NOTE: The committed environment files (.env.development, .env.staging, .env.production)
# contain the actual Supabase credentials. Those files are safe to commit because
# they only contain public-facing Anon Keys. Security is enforced via RLS policies.
