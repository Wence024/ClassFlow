# Environment Variables Example
# This file shows what variables are needed for Vercel (staging) deployment

# === STAGING (VERCEL) CONFIGURATION ===
# Set these in Vercel Project Settings → Environment Variables

VITE_APP_ENV=staging
VITE_SUPABASE_URL=https://pnmzjmcfeekculqyirpr.supabase.co
VITE_SUPABASE_ANON_KEY=your_staging_anon_key_here
VITE_SUPABASE_PROJECT_ID=pnmzjmcfeekculqyirpr

# === DEVELOPMENT ===
# No env variables needed - values are hardcoded in src/lib/runtimeConfig.ts

# === PRODUCTION (HOSTINGER) ===
# No env variables needed - edit public/config.js directly on server
# See public/config.production.template.js for structure
