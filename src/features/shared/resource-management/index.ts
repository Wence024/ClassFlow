/**
 * Shared resource management exports for cross-department request functionality.
 */

export { 
  useResourceRequests, 
  useDepartmentRequests, 
  useMyPendingRequests 
} from './hooks/useResourceRequests';

export { useRequestNotifications } from './hooks/useRequestNotifications';
