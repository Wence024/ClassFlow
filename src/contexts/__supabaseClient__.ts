/**
 * Re-export of the primary Supabase client.
 * This file exists for backward compatibility with older imports.
 * 
 * Prefer importing directly from '@/lib/supabase' in new code.
 */
export { supabase } from '../lib/supabase';
